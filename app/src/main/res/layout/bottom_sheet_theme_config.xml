<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="#121420"
    android:orientation="vertical"
    android:padding="16dp">

    <View
        android:layout_width="36dp"
        android:layout_height="4dp"
        android:layout_gravity="center_horizontal"
        android:layout_marginBottom="12dp"
        android:background="#33334D" />

    <TextView
        android:id="@+id/tv_sheet_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Настройка виджета"
        android:textColor="#FFFFFF"
        android:textSize="16sp"
        android:textStyle="bold" />

    <TextView
        android:id="@+id/tv_preview_subtitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="2dp"
        android:text="Предпросмотр на рабочем столе:"
        android:textColor="#8A8A9E"
        android:textSize="11sp" />

    <!-- Live Preview Frame Container (Fixed height 150dp for neat preview) -->
    <FrameLayout
        android:id="@+id/preview_container_frame"
        android:layout_width="match_parent"
        android:layout_height="150dp"
        android:layout_marginTop="8dp"
        android:layout_marginBottom="12dp"
        android:background="@drawable/preview_wallpaper_bg"
        android:padding="8dp">

        <!-- Dynamic Live Preview Card -->
        <FrameLayout
            android:id="@+id/preview_card_holder"
            android:layout_width="match_parent"
            android:layout_height="match_parent" />

    </FrameLayout>

    <!-- Theme Style Selector Buttons -->
    <TextView
        android:id="@+id/tv_theme_selector_label"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Стиль темы виджета:"
        android:textColor="#A0A0B0"
        android:textSize="12sp" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:layout_marginBottom="12dp"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/btn_theme_dark"
            android:layout_width="0dp"
            android:layout_height="36dp"
            android:layout_weight="1"
            android:background="@drawable/widget_box_bg"
            android:clickable="true"
            android:focusable="true"
            android:gravity="center"
            android:text="🖤 Чистый виджет"
            android:textColor="#FFFFFF"
            android:textSize="11sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/btn_theme_art"
            android:layout_width="0dp"
            android:layout_height="36dp"
            android:layout_marginStart="8dp"
            android:layout_weight="1"
            android:background="@drawable/widget_box_bg"
            android:clickable="true"
            android:focusable="true"
            android:gravity="center"
            android:text="🎮 Тема карточки"
            android:textColor="#F5C342"
            android:textSize="11sp"
            android:textStyle="bold" />
    </LinearLayout>

    <!-- Transparency Slider -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/tv_opacity_label"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Прозрачность фона:"
            android:textColor="#A0A0B0"
            android:textSize="12sp" />

        <TextView
            android:id="@+id/tv_opacity_value"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="15%"
            android:textColor="#FFFFFF"
            android:textSize="12sp"
            android:textStyle="bold" />
    </LinearLayout>

    <SeekBar
        android:id="@+id/seekbar_opacity"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:layout_marginBottom="16dp"
        android:max="100"
        android:progress="15" />

    <!-- Action Button -->
    <Button
        android:id="@+id/btn_confirm_add"
        android:layout_width="match_parent"
        android:layout_height="44dp"
        android:text="➕ Добавить на рабочий стол"
        android:textSize="13sp" />

</LinearLayout>
